{
  "name": "skillnft-stellar-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test",
    "lint": "npm --prefix frontend run lint",
    "dev": "npm --prefix frontend run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
